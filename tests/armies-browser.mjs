import {chromium} from '@playwright/test';import assert from 'node:assert/strict';import {mkdtemp,rm,mkdir} from 'node:fs/promises';import {tmpdir} from 'node:os';import {join} from 'node:path';import {WorldStore,stateHash} from '../dist/apps/server/src/store.js';import {createWorld} from '../dist/packages/worldgen/src/index.js';import {startServer} from '../dist/apps/server/src/index.js';import {applyProposal} from '../dist/packages/engine/src/index.js';import {DEFAULT_SETTLEMENT_SETTINGS} from '../dist/packages/contracts/src/settlement-defaults.js';
const root=await mkdtemp(join(tmpdir(),'logos-armies-browser-')),store=new WorldStore(root);let w=createWorld({id:'first-world',name:'Reinforcement garden',seed:'technology-browser',frequency:2});w.tiles.forEach(t=>t.elevationM=1000);const to=w.cells[0].neighbors[0];w=applyProposal(w,{id:'setup',worldId:w.id,expectedRevision:0,summary:'Friendly garrisons',operations:[{kind:'faction-define',tileId:0,expectedVersion:0,faction:{id:'oak',label:'Oak Circle',color:'#336633'}},...[0,to].flatMap(tileId=>[{kind:'settlement-create',tileId,label:'Village',population:100,foodRations:10000,settings:{...DEFAULT_SETTLEMENT_SETTINGS,growthPermille:0,lossPermille:0}},{kind:'rainfall',tileId,mmPerDay:5},{kind:'temperature',tileId,celsius:20,mode:'sustained'},{kind:'faction-claim',tileId,factionId:'oak'}]),{kind:'garrison-configure',tileId:0,expectedVersion:0,target:60,reserveDays:7}]});await store.save(w);let calls=0;const app=await startServer({root,port:0,provider:{name:'Army fixture',async generate(){calls++;throw Error('No model expected');}}}),base=`http://127.0.0.1:${app.server.address().port}`,browser=await chromium.launch({headless:true,args:['--no-sandbox','--use-gl=angle','--use-angle=swiftshader','--enable-unsafe-swiftshader']});
try{await mkdir('.local/screenshots',{recursive:true});const page=await browser.newPage({viewport:{width:1440,height:1000},reducedMotion:'reduce'}),errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto(base);await page.waitForFunction(()=>document.querySelector('#save-status').textContent.includes('Saved locally'));await page.locator('#globe').focus();await page.keyboard.press('ArrowRight');await page.keyboard.press('ArrowLeft');await page.locator('#army-controls summary').click();const apply=async()=>{await page.locator('#proposal').waitFor({state:'visible'});await page.locator('#apply-proposal').click();await page.locator('#proposal').waitFor({state:'hidden'});};const step=async day=>{await page.locator('#step').click();await page.waitForFunction(n=>document.querySelector('#day').textContent===`DAY ${n}`,day);};
 const hash=stateHash(await store.load(w.id));await page.locator('#army-population').fill('40');await page.locator('#army-food').fill('400');await page.locator('#army-depart').click();await page.locator('#proposal').waitFor({state:'visible'});assert.match(await page.locator('.journey-preview').textContent(),/Apply 200 \/ 40 traveling/);assert.match(await page.locator('.garrison-preview').textContent(),new RegExp(`zone ${to}: target 40`));assert.equal(stateHash(await store.load(w.id)),hash);await page.locator('#cancel-proposal').click();await page.locator('#army-depart').click();await apply();let saved=await store.load(w.id);assert.equal(saved.tiles[0].population,60);assert.equal(saved.tiles[0].garrison.target,20);assert.equal(saved.journeys.active[0].military.factionId,'oak');assert.match(await page.locator('.journey-note').textContent(),/Army of oak/);await step(1);saved=await store.load(w.id);assert.equal(saved.journeys.active[0].foodRations,360);assert.equal((await store.verifyHistory(w.id)).hash,stateHash(saved));await page.locator('#army-controls').scrollIntoViewIfNeeded();await page.screenshot({path:'.local/screenshots/armies-desktop.png'});await page.reload();await page.waitForFunction(()=>document.querySelector('#day').textContent==='DAY 1');assert.equal(stateHash(await store.load(w.id)),stateHash(saved));await page.locator('#globe').focus();await page.keyboard.press('ArrowRight');await page.keyboard.press('ArrowLeft');await page.locator('#army-controls summary').click();await step(2);saved=await store.load(w.id);assert.equal(saved.journeys.active.length,0);assert.equal(saved.tiles[to].population,140);assert.equal(saved.tiles[to].garrison.target,40);assert.equal(saved.tiles[to].settlement.lastDay.consumed,140);assert.equal((await store.verifyHistory(w.id)).hash,stateHash(saved));await page.setViewportSize({width:390,height:844});await page.locator('#army-controls').scrollIntoViewIfNeeded();await page.screenshot({path:'.local/screenshots/armies-mobile.png'});assert.equal(await page.evaluate(()=>document.documentElement.scrollWidth>innerWidth),false);assert.equal(calls,0);assert.deepEqual(errors,[]);console.log('Army browser passed departure review/cancel/apply, exact debits, allegiance, carried meals, arrival/reinforcement, saved transit reload/replay, responsive layouts and no autonomous model calls.');
}finally{await browser.close();await app.close();await rm(root,{recursive:true,force:true});}
