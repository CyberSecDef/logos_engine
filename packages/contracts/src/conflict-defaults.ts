// Shared data-only defaults; browser imports must not initialize validation schemas.
export const DEFAULT_CONFLICT_SETTINGS={deploymentPermille:500,minimumTroops:10,daysPerHop:2,provisionDays:7,cooldownDays:30,lossPermille:100,requiredAdvantagePermille:1250,maxDeparturesPerDay:4};
